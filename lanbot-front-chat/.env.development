# 前端环境变量配置
# Docker Compose 会自动注入这些变量

# WebSocket端口（边缘服务器）
VITE_WS_PORT=20551

# HTTP API端口（边缘服务器）
VITE_API_PORT=20550

# chatbot直接API地址（可选，用于直连调试）
VITE_CHATBOT_API=http://localhost:1090/chatbot/api/v1.0/get